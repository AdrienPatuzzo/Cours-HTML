// structures de contrôle de flux
// ////////////

//nombre <- 6 nombre <- saisie()
let nombre = 0;

// si (nombre > 0) alors
if (nombre > 0) {
    // ecrire("nombre positif");
    console.log("nombre positif");
}    
// sinon si (nombre < 0) alors
else if (nombre < 0) {
    // ecrire ("nombre négatif");
    console.log("nombre négatif");
}    
// sinon
else {
    // ecrire("nombre neutre");
    console.log("nombre neutre");
// fin si
}

